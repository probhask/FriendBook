[build]
  command = "npm run build"
  publish = "dist"

[functions]
  directory = "netlify/functions"
  node_bundler = "esbuild"

# SPA fallback. Must be last; never rewrite the Functions path.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
